import assert from 'node:assert';
import { test } from 'node:test';

// This file contains tests for the legacy guess.js module
// These tests ensure the old module structure still works during migration
// TODO: Remove this file once all functionality has been fully migrated to the new architecture

test('Legacy compatibility test', () => {
    // Placeholder test to ensure the test runner doesn't fail
    // This should be removed once the guess.js file is deleted
    assert(true, 'Legacy test - remove when guess.js is deleted');
});

